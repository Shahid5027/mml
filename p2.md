Generate the complete initial project setup.

Requirements:

Frontend:

React + Vite + TypeScript
TailwindCSS setup
Framer Motion setup
routing setup
reusable layout structure
sidebar/navbar structure

Backend:

Express setup
PostgreSQL connection
Prisma setup
JWT auth setup
environment variables
modular folder structure

Generate:

dependency installation commands
folder creation
base configuration
starter architecture

Do NOT generate business logic yet.